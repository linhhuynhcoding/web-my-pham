// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: products.sql

package repository

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createProduct = `-- name: CreateProduct :one
insert into products(
    name,
    description,
    price,
    image_url,
    stock,
    brand_id,
    category_id,
    buyturn
) values (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7,
    0
) returning id, name, description, price, category_id, stock, buyturn, image_url, brand_id, created_at, updated_at
`

type CreateProductParams struct {
	Name        string         `json:"name"`
	Description pgtype.Text    `json:"description"`
	Price       pgtype.Numeric `json:"price"`
	ImageUrl    string         `json:"image_url"`
	Stock       int32          `json:"stock"`
	BrandID     int32          `json:"brand_id"`
	CategoryID  int32          `json:"category_id"`
}

func (q *Queries) CreateProduct(ctx context.Context, arg CreateProductParams) (Product, error) {
	row := q.db.QueryRow(ctx, createProduct,
		arg.Name,
		arg.Description,
		arg.Price,
		arg.ImageUrl,
		arg.Stock,
		arg.BrandID,
		arg.CategoryID,
	)
	var i Product
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.Price,
		&i.CategoryID,
		&i.Stock,
		&i.Buyturn,
		&i.ImageUrl,
		&i.BrandID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteProduct = `-- name: DeleteProduct :exec

delete from products where id = $1
`

// insert into products(
//
//	id,
//	name,
//	description,
//	price,
//	image_url,
//	stock,
//	brand_id,
//	category_id,
//	buyturn
//
// ) values (
//
//	sqlc.arg('id'),
//	sqlc.narg('name'),
//	sqlc.narg('description'),
//	sqlc.narg('price'),
//	sqlc.narg('image_url'),
//	sqlc.narg('stock'),
//	sqlc.narg('brand_id'),
//	sqlc.narg('category_id'),
//	sqlc.narg('buyturn')
//
// )
// on conflict (id) do update
// set
//
//	name = COALESCE(EXCLUDED.name, products.name),
//	description = COALESCE(EXCLUDED.description, products.description),
//	price = COALESCE(EXCLUDED.price, products.price),
//	image_url = COALESCE(EXCLUDED.image_url, products.image_url),
//	stock = COALESCE(EXCLUDED.stock, products.stock),
//	brand_id = COALESCE(EXCLUDED.brand_id, products.brand_id),
//	category_id = COALESCE(EXCLUDED.category_id, products.category_id),
//	buyturn = COALESCE(EXCLUDED.buyturn, products.buyturn);
func (q *Queries) DeleteProduct(ctx context.Context, id int32) error {
	_, err := q.db.Exec(ctx, deleteProduct, id)
	return err
}

const getProductByCateID = `-- name: GetProductByCateID :many
SELECT p.id, p.name, p.description, p.price, p.category_id, p.stock, p.buyturn, p.image_url, p.brand_id, p.created_at, p.updated_at
FROM products p
WHERE p.category_id = $1 limit $2 offset $3
`

type GetProductByCateIDParams struct {
	CategoryID int32 `json:"category_id"`
	Limit      int32 `json:"limit"`
	Offset     int32 `json:"offset"`
}

func (q *Queries) GetProductByCateID(ctx context.Context, arg GetProductByCateIDParams) ([]Product, error) {
	rows, err := q.db.Query(ctx, getProductByCateID, arg.CategoryID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Product{}
	for rows.Next() {
		var i Product
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Description,
			&i.Price,
			&i.CategoryID,
			&i.Stock,
			&i.Buyturn,
			&i.ImageUrl,
			&i.BrandID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getProductDetail = `-- name: GetProductDetail :one
SELECT
    p.id, p.name, p.description, p.price, p.category_id, p.stock, p.buyturn, p.image_url, p.brand_id, p.created_at, p.updated_at,
    b.name as brand_name,
    b.image_url as brand_image_url,
    c.name as category_name,
    c.image_url as category_image_url
FROM
    products p
LEFT JOIN brands b ON p.brand_id = b.id
LEFT JOIN categories c ON p.category_id = c.id
WHERE
    p.id = $1
`

type GetProductDetailRow struct {
	ID               int32            `json:"id"`
	Name             string           `json:"name"`
	Description      pgtype.Text      `json:"description"`
	Price            pgtype.Numeric   `json:"price"`
	CategoryID       int32            `json:"category_id"`
	Stock            int32            `json:"stock"`
	Buyturn          int32            `json:"buyturn"`
	ImageUrl         string           `json:"image_url"`
	BrandID          int32            `json:"brand_id"`
	CreatedAt        pgtype.Timestamp `json:"created_at"`
	UpdatedAt        pgtype.Timestamp `json:"updated_at"`
	BrandName        pgtype.Text      `json:"brand_name"`
	BrandImageUrl    pgtype.Text      `json:"brand_image_url"`
	CategoryName     pgtype.Text      `json:"category_name"`
	CategoryImageUrl pgtype.Text      `json:"category_image_url"`
}

func (q *Queries) GetProductDetail(ctx context.Context, id int32) (GetProductDetailRow, error) {
	row := q.db.QueryRow(ctx, getProductDetail, id)
	var i GetProductDetailRow
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.Price,
		&i.CategoryID,
		&i.Stock,
		&i.Buyturn,
		&i.ImageUrl,
		&i.BrandID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.BrandName,
		&i.BrandImageUrl,
		&i.CategoryName,
		&i.CategoryImageUrl,
	)
	return i, err
}

const listProducts = `-- name: ListProducts :many
SELECT
    p.id, p.name, p.description, p.price, p.category_id, p.stock, p.buyturn, p.image_url, p.brand_id, p.created_at, p.updated_at,
    b.name as brand_name,
    b.image_url as brand_image_url,
    c.name as category_name,
    c.image_url as category_image_url,
    count(*) OVER() AS total_records
FROM
    products p
JOIN
    brands b ON p.brand_id = b.id
JOIN
    categories c ON p.category_id = c.id
WHERE
    ($3::text IS NULL OR p.name ILIKE '%' || $3::text || '%')
    AND ($4::float IS NULL OR p.price >= $4::float)
    AND ($5::float IS NULL OR p.price <= $5::float)
    AND (
        $6::int[] IS NULL 
        OR cardinality($6::int[]) = 0 
        OR p.brand_id = ANY($6::int[])
    )
    AND (
        $7::int[] IS NULL 
        OR cardinality($7::int[]) = 0 
        OR p.category_id = ANY($7::int[])
    )
ORDER BY
    CASE WHEN $8::text = 'price_asc' THEN p.price END ASC,
    CASE WHEN $8::text = 'price_desc' THEN p.price END DESC,
    CASE WHEN $8::text = 'best_seller' THEN p.buyturn END DESC,
    p.id ASC
LIMIT $1
OFFSET $2
`

type ListProductsParams struct {
	Limit       int32         `json:"limit"`
	Offset      int32         `json:"offset"`
	Keyword     pgtype.Text   `json:"keyword"`
	MinPrice    pgtype.Float8 `json:"min_price"`
	MaxPrice    pgtype.Float8 `json:"max_price"`
	BrandIds    []int32       `json:"brand_ids"`
	CategoryIds []int32       `json:"category_ids"`
	OrderBy     string        `json:"order_by"`
}

type ListProductsRow struct {
	ID               int32            `json:"id"`
	Name             string           `json:"name"`
	Description      pgtype.Text      `json:"description"`
	Price            pgtype.Numeric   `json:"price"`
	CategoryID       int32            `json:"category_id"`
	Stock            int32            `json:"stock"`
	Buyturn          int32            `json:"buyturn"`
	ImageUrl         string           `json:"image_url"`
	BrandID          int32            `json:"brand_id"`
	CreatedAt        pgtype.Timestamp `json:"created_at"`
	UpdatedAt        pgtype.Timestamp `json:"updated_at"`
	BrandName        pgtype.Text      `json:"brand_name"`
	BrandImageUrl    pgtype.Text      `json:"brand_image_url"`
	CategoryName     string           `json:"category_name"`
	CategoryImageUrl pgtype.Text      `json:"category_image_url"`
	TotalRecords     *int64           `json:"total_records"`
}

func (q *Queries) ListProducts(ctx context.Context, arg ListProductsParams) ([]ListProductsRow, error) {
	rows, err := q.db.Query(ctx, listProducts,
		arg.Limit,
		arg.Offset,
		arg.Keyword,
		arg.MinPrice,
		arg.MaxPrice,
		arg.BrandIds,
		arg.CategoryIds,
		arg.OrderBy,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListProductsRow{}
	for rows.Next() {
		var i ListProductsRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Description,
			&i.Price,
			&i.CategoryID,
			&i.Stock,
			&i.Buyturn,
			&i.ImageUrl,
			&i.BrandID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.BrandName,
			&i.BrandImageUrl,
			&i.CategoryName,
			&i.CategoryImageUrl,
			&i.TotalRecords,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateProduct = `-- name: UpdateProduct :exec
update products 
set 
    name = case when $2::text is not null then $2 else name end,  
    description = case when $3::text is not null then $3 else description end,
    price = case when $4::numeric is not null then $4 else price end,
    image_url = case when $5::text is not null then $5 else image_url end,
    stock = case when $6::int is not null then $6 else stock end,
    brand_id = case when $7::int is not null then $7 else brand_id end,
    category_id = case when $8::int is not null then $8 else category_id end,
    buyturn = case when $9::int is not null then $9 else buyturn end
WHERE id = $1
`

type UpdateProductParams struct {
	ID          int32          `json:"id"`
	Name        pgtype.Text    `json:"name"`
	Description pgtype.Text    `json:"description"`
	Price       pgtype.Numeric `json:"price"`
	ImageUrl    pgtype.Text    `json:"image_url"`
	Stock       pgtype.Int4    `json:"stock"`
	BrandID     pgtype.Int4    `json:"brand_id"`
	CategoryID  pgtype.Int4    `json:"category_id"`
	Buyturn     pgtype.Int4    `json:"buyturn"`
}

func (q *Queries) UpdateProduct(ctx context.Context, arg UpdateProductParams) error {
	_, err := q.db.Exec(ctx, updateProduct,
		arg.ID,
		arg.Name,
		arg.Description,
		arg.Price,
		arg.ImageUrl,
		arg.Stock,
		arg.BrandID,
		arg.CategoryID,
		arg.Buyturn,
	)
	return err
}

const updateProductStock = `-- name: UpdateProductStock :exec
update products set stock = stock - $2::int, buyturn = buyturn + $2::int where id = $1
`

type UpdateProductStockParams struct {
	ID       int32 `json:"id"`
	Quantity int32 `json:"quantity"`
}

func (q *Queries) UpdateProductStock(ctx context.Context, arg UpdateProductStockParams) error {
	_, err := q.db.Exec(ctx, updateProductStock, arg.ID, arg.Quantity)
	return err
}
